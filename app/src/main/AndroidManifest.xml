<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projet_vente_voiture">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projet_vente_voiture">
        <activity android:name=".Mes_annonces"></activity>
        <activity android:name=".Inscription" />
        <activity android:name=".Connexion" />
        <activity android:name=".Ajouter_annonce" />
        <activity android:name=".Detaille" />
        <activity android:name=".Recherche">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>